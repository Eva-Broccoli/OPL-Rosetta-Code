REM All printable characters on Psion devices (up to Series 5 as far as I know) are 1 byte, so byte length is always the same as character length.

PROC main:
  LOCAL s$(100)
  PRINT "Please enter a string."
  INPUT s$
  PRINT "Both character and byte length are",LEN(s$)
  GET
ENDP
